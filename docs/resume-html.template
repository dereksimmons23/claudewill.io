<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>$if(title)$$title$$else$Derek Simmons | Strategic Builder$endif$</title>
    <meta name="description" content="$if(description)$$description$$else$Executive leader bridging human wisdom with technological advancement - 20M-plus revenue through innovative frameworks$endif$">
    <meta name="keywords" content="AI, leadership, digital transformation, Derek Simmons, resume, executive, ethics">
    
    <!-- Open Graph Meta Tags -->
    <meta property="og:title" content="$if(title)$$title$$else$Derek Simmons | Strategic Builder$endif$">
    <meta property="og:description" content="$if(description)$$description$$else$Executive leader bridging human wisdom with technological advancement - 20M-plus revenue through innovative frameworks$endif$">
    <meta property="og:type" content="website">
    <meta property="og:url" content="https://claudewill.io/resume">
    <meta property="og:image" content="https://claudewill.io/og-image.png">
    
    <!-- Twitter Card Meta Tags -->
    <meta name="twitter:card" content="summary_large_image">
    <meta name="twitter:title" content="$if(title)$$title$$else$Derek Simmons | Strategic Builder$endif$">
    <meta name="twitter:description" content="$if(description)$$description$$else$Executive leader bridging human wisdom with technological advancement - 20M-plus revenue through innovative frameworks$endif$">
    <meta name="twitter:image" content="https://claudewill.io/og-image.png">
    
    <!-- Fonts and Icons -->
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link rel="preload" as="style" href="https://fonts.googleapis.com/css2?family=Noto+Sans:wght@400;500;700&family=Noto+Serif:wght@400;700&display=swap">
    <link href="https://fonts.googleapis.com/css2?family=Noto+Sans:wght@400;500;700&family=Noto+Serif:wght@400;700&display=swap" rel="stylesheet">
    <link href="https://fonts.googleapis.com/icon?family=Material+Icons" rel="stylesheet">
    
    <!-- CSS will be in a separate file -->
    <link href="css/resume-styles.css" rel="stylesheet">
    
    <link rel="icon" href="/favicon.ico" type="image/x-icon">
    <style>
        @media print {
            .site-header, .resume-navigation, .print-button, .back-button, footer { display: none !important; }
            body { background: #fff !important; color: #000 !important; }
        }
        .tab-content { display: block; }
        .tab-content:not(.active) { display: none; }
        .js-enabled .tab-content { display: none; }
        .js-enabled .tab-content.active { display: block; }
    </style>
</head>
<body>
    <noscript>
        <div style="background: #fff3cd; color: #856404; padding: 1em; border: 1px solid #ffeeba; margin-bottom: 1em;">
            JavaScript is disabled. All resume sections are shown below.
        </div>
    </noscript>
    <!-- Header with wordmark and theme toggle -->
    <header class="site-header" aria-label="Site header">
        <a href="index.html" class="wordmark">claudewill</a>
        <nav class="main-nav">
            <a href="index.html">Home</a>
            <a href="about.html">About Derek</a>
            <a href="projects.html">Projects</a>
            <a href="resume.html">Resume</a>
            <a href="documentation.html">Documentation</a>
        </nav>
        <button id="theme-toggle" aria-label="Toggle theme" tabindex="0">
            <span class="material-icons theme-toggle-icon">light_mode</span>
        </button>
    </header>
    <main>
        <div class="container">
            <section class="resume-header">
                <h1>DEREK SIMMONS</h1>
                <p class="tagline">Transformative Executive | Founder @ CWS | Ethical AI & Digital Strategy</p>
                <div class="contact-info">
                    <div class="contact-item"><span class="material-icons">location_on</span><span>Minnesota / U.S.</span></div>
                    <div class="contact-item"><span class="material-icons">phone</span><a href="tel:2133275683">213-327-5683</a></div>
                    <div class="contact-item"><span class="material-icons">email</span><a href="mailto:simmons.derek@gmail.com">simmons.derek@gmail.com</a></div>
                    <div class="contact-item"><span class="material-icons">public</span><a href="https://claudewill.io" target="_blank" rel="noopener">claudewill.io</a></div>
                    <div class="contact-item"><span class="material-icons">business</span><a href="https://linkedin.com/in/dereksimm" target="_blank" rel="noopener">linkedin.com/in/dereksimm</a></div>
                </div>
            </section>
            <div class="summary">
                <p>$if(summary)$$summary$$endif$</p>
            </div>
            <div class="resume-navigation" aria-label="Resume navigation" role="tablist">
                <div class="tab-buttons">
                    <button class="tab-button active" data-tab="experience" role="tab" aria-selected="true" tabindex="0" id="tab-experience">Professional Experience</button>
                    <button class="tab-button" data-tab="technical" role="tab" aria-selected="false" tabindex="-1" id="tab-technical">Technical Expertise</button>
                    <button class="tab-button" data-tab="highlights" role="tab" aria-selected="false" tabindex="-1" id="tab-highlights">Career Highlights</button>
                    <button class="tab-button" data-tab="ai-research" role="tab" aria-selected="false" tabindex="-1" id="tab-ai-research">AI Research</button>
                    <button class="tab-button" data-tab="community" role="tab" aria-selected="false" tabindex="-1" id="tab-community">Community Impact</button>
                    <button class="tab-button" data-tab="education" role="tab" aria-selected="false" tabindex="-1" id="tab-education">Education</button>
                </div>
            </div>
            <button class="print-button" onclick="window.print()" aria-label="Print or Save as PDF" tabindex="0">
                <span class="material-icons">print</span>
                Print or Save as PDF
            </button>
            <!-- Tabbed content sections. Use Pandoc Divs or raw HTML in Markdown to target these IDs/classes. -->
            <div class="tab-content active" id="experience-tab" role="tabpanel" aria-labelledby="tab-experience">
                $if(experience)$$experience$$endif$
            </div>
            <div class="tab-content" id="technical-tab" role="tabpanel" aria-labelledby="tab-technical">
                $if(technical)$$technical$$endif$
            </div>
            <div class="tab-content" id="highlights-tab" role="tabpanel" aria-labelledby="tab-highlights">
                $if(highlights)$$highlights$$endif$
            </div>
            <div class="tab-content" id="ai-research-tab" role="tabpanel" aria-labelledby="tab-ai-research">
                $if(ai-research)$$ai-research$$endif$
            </div>
            <div class="tab-content" id="community-tab" role="tabpanel" aria-labelledby="tab-community">
                $if(community)$$community$$endif$
            </div>
            <div class="tab-content" id="education-tab" role="tabpanel" aria-labelledby="tab-education">
                $if(education)$$education$$endif$
            </div>
            <a href="https://claudewill.io" class="back-button">
                <span class="material-icons">arrow_back</span>
                Back to Claude Will
            </a>
        </div>
    </main>
    <footer>
        <div class="container">
            <p>© 2025 Derek Simmons |
                <a href="/documentation.html">Documentation</a> |
                <a href="/about.html">About Derek</a> |
                <a href="/documentation.html#privacy">Privacy Policy</a> |
                <a href="/documentation.html#terms">Terms of Use</a>
            </p>
        </div>
    </footer>
    <script>
        // Theme toggle functionality and tab navigation
        document.addEventListener('DOMContentLoaded', () => {
            const themeToggle = document.getElementById('theme-toggle');
            const themeIcon = themeToggle.querySelector('.theme-toggle-icon');
            try {
                const storedTheme = localStorage.getItem('theme');
                if (storedTheme === 'dark') {
                    document.body.classList.add('dark-theme');
                    themeIcon.textContent = 'light_mode';
                }
            } catch (error) {
                console.log('Unable to access localStorage. Using default theme.');
            }
            themeToggle.addEventListener('click', () => {
                document.body.classList.toggle('dark-theme');
                if (document.body.classList.contains('dark-theme')) {
                    themeIcon.textContent = 'light_mode';
                    try {
                        localStorage.setItem('theme', 'dark');
                    } catch (error) {
                        console.log('Unable to access localStorage.');
                    }
                } else {
                    themeIcon.textContent = 'dark_mode';
                    try {
                        localStorage.setItem('theme', 'light');
                    } catch (error) {
                        console.log('Unable to access localStorage.');
                    }
                }
            });
            // Tab functionality
            const tabButtons = document.querySelectorAll('.tab-button');
            const tabContents = document.querySelectorAll('.tab-content');
            tabButtons.forEach((button, idx) => {
                button.addEventListener('click', () => {
                    tabButtons.forEach(b => {
                        b.classList.remove('active');
                        b.setAttribute('aria-selected', 'false');
                        b.setAttribute('tabindex', '-1');
                    });
                    tabContents.forEach(c => c.classList.remove('active'));
                    button.classList.add('active');
                    button.setAttribute('aria-selected', 'true');
                    button.setAttribute('tabindex', '0');
                    button.focus();
                    const tabId = button.getAttribute('data-tab');
                    document.getElementById(`${tabId}-tab`).classList.add('active');
                });
                button.addEventListener('keydown', (e) => {
                    let newIdx = null;
                    if (e.key === 'ArrowRight' || e.key === 'ArrowDown') {
                        newIdx = (idx + 1) % tabButtons.length;
                    } else if (e.key === 'ArrowLeft' || e.key === 'ArrowUp') {
                        newIdx = (idx - 1 + tabButtons.length) % tabButtons.length;
                    }
                    if (newIdx !== null) {
                        e.preventDefault();
                        tabButtons[newIdx].click();
                    }
                });
            });
            document.body.classList.add('js-enabled');
        });
    </script>
</body>
</html> 