// Generated by CoffeeScript 1.3.3
var PresenceValidator;

module.exports = PresenceValidator = (function() {

  PresenceValidator.detectBy = ['presence'];

  function PresenceValidator(params) {
    this.params = params;
  }

  PresenceValidator.prototype.validate = function(value, valid) {
    if (!this.params.presence) {
      return valid(true);
    }
    if (!value && !this.params.allowBlank) {
      return valid(false, 'is not present');
    } else {
      return valid(true);
    }
  };

  return PresenceValidator;

})();
