// Generated by CoffeeScript 1.3.3
var NumericalityValidator;

module.exports = NumericalityValidator = (function() {

  NumericalityValidator.detectBy = ['numericality'];

  function NumericalityValidator(params) {
    this.params = params;
  }

  NumericalityValidator.prototype.validate = function(value, valid) {
    var NumberValueValidator;
    if (this.params.numericality && 'number' === typeof value) {
      if (this.params.numericality.onlyInteger) {
        if (parseInt(value) === value) {
          if (this.params.numericality.value) {
            NumberValueValidator = require('./number_value');
            return (new NumberValueValidator({
              value: this.params.numericality.value
            })).validate(value, valid);
          } else {
            return valid(true);
          }
        } else {
          return valid(false, 'is not integer');
        }
      } else {
        return valid(true);
      }
    } else {
      return valid(false, 'is not number');
    }
  };

  return NumericalityValidator;

})();
